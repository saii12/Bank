<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tenco.bank.repository.interfaces.HistoryRepository">
	
	<!-- db에 now default 설정해놔서 created_at insert 안해도 됨 -->
	<insert id="insert">
		insert into history_tb 
			(amount, w_account_id, d_account_id, w_balance, d_balance)
		values (#{amount}, #{wAccountId}, #{dAccountId}, #{wBalance}, #{dBalance})
	</insert>
	
	<update id="update" parameterType="com.tenco.bank.repository.entity.History"> <!-- parameterType 생략가능 -->
		update history_tb set 
					amount = #{amount},
                    w_account_id = #{wAccountId},
                    d_account_id = #{dAccountId},
                    w_balance = #{wBalance},
                    d_balance = #{dBalance}
                    where id = #{id}
	</update>
	
	<delete id="delete" parameterType="int"> <!-- parameterType 생략가능 -->
		delete from history_tb where id = #{id}
	</delete>
	
	<select id="findById" resultType="com.tenco.bank.repository.entity.History">
		select * from history_tb where id = #{id}
	</select>
	
	<select id="findAll" resultType="com.tenco.bank.repository.entity.History">
		select * from history_tb
	</select>

</mapper>